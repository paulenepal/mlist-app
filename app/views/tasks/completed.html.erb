<%= render "layouts/navbar" %>

<div class="spacing">
  <div class="main-container">

      <div>
        <h1>💃 Completed Tasks</h1>
        <% if @tasks_completed.length > 0 %>
            <% @tasks_completed.each do |task| %>
                <%= render "task_common", task: task %>
            <% end %>
        <% else %>
            <br />
            <div>You have no task completed.</div>
        <% end  %>
    </div>

  </div>
</div>